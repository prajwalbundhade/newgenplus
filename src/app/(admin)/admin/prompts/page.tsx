/**
 * Admin prompts management — /admin/prompts
 * Authorization enforced by AdminLayout.
 */

export const metadata = { title: 'Prompts — NewGenPlus Admin' }

export default function AdminPromptsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[#111111]">Prompts</h1>
      <p className="mt-2 text-sm text-gray-500">Prompt management coming soon.</p>
    </div>
  )
}
