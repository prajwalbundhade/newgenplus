/**
 * Admin categories management — /admin/categories
 * Authorization enforced by AdminLayout.
 */

export const metadata = { title: 'Categories — NewGenPlus Admin' }

export default function AdminCategoriesPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[#111111]">Categories</h1>
      <p className="mt-2 text-sm text-gray-500">Category management coming soon.</p>
    </div>
  )
}
