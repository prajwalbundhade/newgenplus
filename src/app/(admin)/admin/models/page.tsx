/**
 * Admin models management — /admin/models
 * Authorization enforced by AdminLayout.
 */

export const metadata = { title: 'Models — NewGenPlus Admin' }

export default function AdminModelsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[#111111]">Models</h1>
      <p className="mt-2 text-sm text-gray-500">Model management coming soon.</p>
    </div>
  )
}
